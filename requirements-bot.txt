smtplib  # ya viene con Python, pero puedes usar yagmail si prefieres
yagmail==0.15.293  # opcional, para envío fácil de correos con Gmail
python-dotenv==1.0.1  # para gestionar tus credenciales desde un .env
